#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int numbers = 48;

while (numbers <= 57)
{
putchar(numbers);
numbers++;
}
putchar('\n');
return (0);
}
